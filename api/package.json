{
  "name": "artisan-dashboard-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .nexus/ api.graphql",
    "dev": "nexus dev",
    "build": "npm run clean && nexus build",
    "start": "node .nexus/build/src"
  },
  "dependencies": {
    "acorn": "^6.0.1",
    "nanoid": "^3.1.12",
    "nexus": "0.26.1",
    "nexus-plugin-prisma": "^0.17.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "typescript": "^3.9.7"
  }
}
